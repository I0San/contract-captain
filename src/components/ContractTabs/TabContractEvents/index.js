import React from 'react'
import { GridEvents } from '../../DataTable/gridEvents'


const TabContractEvents = ({ contract }) => {
    return (
        <GridEvents contract={contract} />
    )
}

export default TabContractEvents